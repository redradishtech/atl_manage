#!/bin/bash -eu

# shellcheck source=/opt/atl_manage/lib/common.sh
source "$ATL_MANAGE/lib/common.sh" --

log "Validation: ensure the From: address matches the reverse lookup of our IP, to avoid JIRA email being flagged as spam."
log "Validation: ensure account password expiry is OFF"
