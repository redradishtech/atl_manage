#!/bin/bash -eu

# Finds the 10 most recently modified files in a directory.
# Useful for verifying that the latest content has been copied/backed up.

#https://stackoverflow.com/questions/4561895/how-to-recursively-find-the-latest-modified-file-in-a-directory

find ${1:-.} -type f -printf '%T@ %p\n' | sort -n | tail -10 | cut -f2- -d" "
